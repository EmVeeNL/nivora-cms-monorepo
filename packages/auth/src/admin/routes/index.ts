export function adminRouteFactory() {
	// Admin routes for /admin/users and /admin/roles are injected by the
	// module registry generator. Concrete TanStack Router route objects
	// are created in apps/admin via route code-gen (step 10).
	return [];
}
